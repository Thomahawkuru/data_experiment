%% Devide data per level

function levels = Levels(Experiment)
    trails = max(Experiment.TrailNumber);
    levles = max(Experiment.LevelNumber);
    
    for i = 1:length(Experiment.Time)
        if Experiment.LevelNumber(i) >          
    end    
end